pipeline {
    agent any
    stages {
        stage('install-pip-deps') {
            steps {
                script {
                    sh 'echo Installing dependencies... AKA TESTS TESTS TESTS'
                }
            }
        }
    }
}
